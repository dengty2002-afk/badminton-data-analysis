# RTX 5060 / Blackwell Windows runtime (Python 3.12).
# Install torch/torchvision from the official cu130 index first:
#   pip install -r requirements-models-cuda.txt --index-url https://download.pytorch.org/whl/cu130
torch==2.13.0+cu130
torchvision==0.28.0+cu130

# Then install the application layer from PyPI; the pinned torch above is reused.
#   pip install ultralytics==8.4.116
